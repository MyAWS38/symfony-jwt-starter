{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    <div class="container">
        <div class="card card-block">
            <div class="row">
                <div class="col-md-12">
                    <h3>Delete User</h3>
                        <p class="text-md-center">Are you sure you would like to delete this user?</p>
                        <h3 class="text-md-center">{{ user.email }}</h3>
                </div>
            </div>
            <div class="row">
                <div class="col-sm-12 text-right">
                    <div class="btn-group" role="group" aria-label="Actions">
                        <a href="{{ path('admin_user_list') }}" class="btn btn-secondary">Cancel</a>
                        <button type="submit" class="btn btn-danger" formnovalidate>Delete</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}
